#include <stdio.h>
#include <math.h>

int funcao1(float x) {
    float num = 0; x = 0;
    printf("digite um numero real: ");
    scanf("%f" &num);
    if(num > 0 && num < 1) {
        x = num;
    }
    return num;
}

int arctan(float aprox) {
    x = funcao1(x);
    
}

int funcao3(float x, char ) {
    
}


int main () {
    int a;
    imprimir = funcao3(a);
    printf
}
